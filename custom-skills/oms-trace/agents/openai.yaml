interface:
  display_name: "Trace"
  short_description: "Explain failures before changing code"
  default_prompt: "Use $oms-trace to explain this failure with competing hypotheses and a discriminating probe before changing anything."
