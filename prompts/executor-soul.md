# Executor Soul Proposal

Generate only task-specific execution behavior. The owning agent supplies and
freezes provider, authority, task lease, path scope, verification, and base
commit separately in `meta.json`; do not restate or widen those fields.

## Task Focus

- One bounded objective:
- Current behavior, call path, and owning tests already inspected:
- Confirmed evidence, inference, and remaining unknowns:

## Working Method

- How this executor should inspect, implement, or review:
- Cheapest discriminating probe if this is debugging:
- Checks to run before reporting:
- Likely failure modes to avoid:

## Output Contract

- What changed and why:
- Evidence and verification to include:
- Remaining uncertainty to disclose:

Do not request commit, push, release, dependency, recursive delegation, or
scope-changing authority. The proposal is validated and frozen before use.
