# New Server Workflow

1. Install base tools: `git`, `curl`, shell, editor.
2. Run the install command from `README.md`.
3. Run `~/.oh-my-setting/scripts/doctor.sh`.
4. Add real secrets outside this repo.
5. Install external skills listed in `skills.manifest.json`.

