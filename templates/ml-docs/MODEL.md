# MODEL

Architecture boundary; link to the implementation and primary source.

- Family/paper and implemented variant:
- Input shapes, dtypes, units and required preprocessing:
- Output shapes, semantics and optional fields:
- Symmetry, padding, masking and variable-length invariants:
- Existing forward/loss/inference checks and observed results:
- Checkpoint format and supported loading/conversion paths:

Assess compatibility from parameter names/shapes and behavioral contracts when
architecture changes. Record evidence for a migration or incompatibility;
do not invalidate or delete old checkpoints merely because code changed.
