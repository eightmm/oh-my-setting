# CONFIGS

Optional configuration boundary. The existing schema/parser is authoritative;
do not maintain a second table of every key and default here.

- Schema and validation entrypoint:
- Configuration files and precedence:
- Required inputs and important constraints:
- Effective configuration snapshot for a run:
- Compatibility or migration requirements when keys change:

Record adopted project conventions, not an assumed optimizer, logging service,
file format, or set of hyperparameters.
