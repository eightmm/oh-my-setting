# REPRODUCIBILITY

Record a verified reproduction path for a reported result.

- Existing run/evaluation commands and expected outputs:
- Code revision and dirty-state provenance:
- Dependency lock/environment, data snapshot and preprocessing:
- Effective configuration, seed and checkpoint identity:
- Hardware/runtime constraints that affect the claim:
- Nondeterminism and acceptable numeric/statistical tolerance:

Use the project's actual framework and execution environment. Determinism
settings are choices, not guarantees of cross-device parity.
Keep machine-private paths out of published records.
